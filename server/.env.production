# 部署前请修改此文件
# ⚠️ 不要将此文件提交到 Git！

# 服务器端口（Render 会自动设置）
PORT=5000

# 运行环境
NODE_ENV=production

# MongoDB 连接字符串
MONGODB_URI=mongodb+srv://weixinsean_db_user:QstPNBA50OL4XKmr@admin.4y7rwbd.mongodb.net/researchlink?retryWrites=true&w=majority

# JWT 密钥（用于生成 Token）
# ⚠️ 生产环境必须使用强随机密钥！
JWT_SECRET=53fba781-ce79-4100-930f-8c09674b8dce

# 文件上传配置
MAX_FILE_SIZE=52428800
UPLOAD_PATH=./uploads

# CORS 配置（部署后修改为前端域名）
# 暂时留空，部署 Vercel 后再填写
CORS_ORIGIN=
